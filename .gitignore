# 데이터 — git에 절대 커밋 금지 (대용량 바이너리는 오브젝트 스토리지로)
*.zip
*.tar
*.tar.gz
*.part
data/
datasets/
downloads/
raw/
*.jpg
*.jpeg
*.png

# 실행 로그
agents/runs/*.jsonl
*.log
result.txt
progress.log

# 재생성 가능한 대용량(버킷이 원본) — 색인 입력·산출은 GCS로
rag_units.jsonl
corpus_dl/
idx_*/

# 포트폴리오·복기 문서는 로컬만(USER 선호) — knowledge/ 미게시
knowledge/

# 파이썬
__pycache__/
*.pyc
.venv/
venv/

# 환경/시크릿
.env
*.key
*.pem

# OS
.DS_Store
