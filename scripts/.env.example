# scripts/.env 템플릿 — 이 파일을 복사해 scripts/.env 로 저장하고 값 채울 것.
# 실제 .env 는 .gitignore에 등록되어 깃에 안 올라감(비밀키 보호).
# aihub_ingest.sh 가 시작 시 자동 source 함 → 매번 export 불필요.

KEY='여기에_AIHUB_API_KEY'
BK='gs://constgx-aihub-237'

# (선택) 다운로드 속도 추정용 — AI Hub throttle 실측 ~12
# RATE_MBs=12
